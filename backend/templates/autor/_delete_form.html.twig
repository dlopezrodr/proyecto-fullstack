<form method="post" action="{{ path('app_autor_delete', {'autor_id': autor.autor_id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ autor.autor_id) }}">
    <button class="btn">Delete</button>
</form>
