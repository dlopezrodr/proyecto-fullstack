{% extends 'base.html.twig' %}

{% block title %}Editorial{% endblock %}

{% block body %}
    <h1>Editorial</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Editorial_id</th>
                <td>{{ editorial.editorialId }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ editorial.nombre }}</td>
            </tr>
            <tr>
                <th>Pais</th>
                <td>{{ editorial.pais }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_editorial_index') }}">back to list</a>

    <a href="{{ path('app_editorial_edit', {'editorial_id': editorial.editorial_id}) }}">edit</a>

    {{ include('editorial/_delete_form.html.twig') }}
{% endblock %}
