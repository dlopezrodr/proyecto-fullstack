<form method="post" action="{{ path('app_editorial_delete', {'editorial_id': editorial.editorial_id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ editorial.editorial_id) }}">
    <button class="btn">Delete</button>
</form>
