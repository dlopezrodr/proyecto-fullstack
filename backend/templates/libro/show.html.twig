{% extends 'base.html.twig' %}

{% block title %}Libro{% endblock %}

{% block body %}
    <h1>Libro</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Libro_id</th>
                <td>{{ libro.libroId }}</td>
            </tr>
            <tr>
                <th>Titulo</th>
                <td>{{ libro.titulo }}</td>
            </tr>
            <tr>
                <th>Isbn</th>
                <td>{{ libro.isbn }}</td>
            </tr>
            <tr>
                <th>Fecha_publicacion</th>
                <td>{{ libro.fechaPublicacion ? libro.fechaPublicacion|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_libro_index') }}">back to list</a>

    <a href="{{ path('app_libro_edit', {'libro_id': libro.libro_id}) }}">edit</a>

    {{ include('libro/_delete_form.html.twig') }}
{% endblock %}
